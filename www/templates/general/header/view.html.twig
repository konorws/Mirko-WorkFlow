<div class="nav_menu">
    <div class="nav toggle">
        <a id="menu_toggle"><i class="fa fa-bars"></i></a>
    </div>
    <nav class="nav navbar-nav">
        <ul class=" navbar-right">
            <li class="nav-item dropdown open" style="padding-left: 15px;">
                <a href="javascript:;"
                   class="user-profile dropdown-toggle"
                   aria-haspopup="true"
                   id="navbarDropdown"
                   data-toggle="dropdown"
                   aria-expanded="false"
                >
                    <img src="{{ attachmentThumb(app.user.image) }}" alt="{{ app.user.fullName }}">
                    {{ app.user.fullName }}
                </a>
                <div class="dropdown-menu dropdown-usermenu pull-right" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ url('user.profile.view', {'user_id': app.user.id}) }}">
                        {% trans %}Profile{% endtrans %}
                    </a>
                    <a class="dropdown-item" href="{{ url('user.security.logout')}}">
                        <i class="fa fa-sign-out pull-right"></i> {% trans %}Log Out{% endtrans %}
                    </a>
                </div>
            </li>
        </ul>
    </nav>
</div>
